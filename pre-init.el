;;; pre-init.el --- DESCRIPTION -*- no-byte-compile: t; lexical-binding: t; -*-

;; size
;; (add-to-list 'default-frame-alist '(width . 100))  ; Set width in character units
;; (add-to-list 'default-frame-alist '(height . 40))  ; Set height in line units

(setq default-frame-alist
             '(
	           ;; (alpha-background . 92) ;; transparency
               (width . 100)
               (height . 40)
               ))
